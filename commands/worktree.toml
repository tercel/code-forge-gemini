description = "Create an isolated git worktree."
prompt = "Activating the code-forge skill: worktree. Please process the following input or context: {{args}}. If no input is provided, proceed with the default workflow for worktree."
